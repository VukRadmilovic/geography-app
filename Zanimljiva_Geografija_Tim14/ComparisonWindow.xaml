<Window x:Class="Zanimljiva_Geografija_Tim14.ComparisonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Country Comparison" Height="650" Width="950" Icon="/earth_icon.png">
    <Grid x:Name="grid">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Grid.Row="0" x:Name="countryGrid">
            <!-- Countries will be added dynamically here -->
        </Grid>
        <Grid Grid.Row="1">
            <Button MaxWidth="200" Click="BackButton_Click" Content="Go back" Margin="0 10" Padding="20 5" BorderBrush="#87d1ee" Background="#87d1ee" Style="{StaticResource btnRound}" FontWeight="DemiBold"></Button>
        </Grid>
    </Grid>
</Window>
